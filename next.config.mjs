/** @type {import('next').NextConfig} */
const nextConfig = {
  // add hostname for profile url firebase
  images: {
    domains: ['lh3.googleusercontent.com', 'ui-avatars.com'],
  },
};

export default nextConfig;
